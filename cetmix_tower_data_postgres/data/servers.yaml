module: cetmix_tower_data_postgres
servers:
  template-postgres:
    variables:
      # Standard server variables
      postgres_image_name: "{{ postgres_instance_name }}:16"
      postgres_instance_name: "shared-postgres"

      # Postgres server variables
