module: cetmix_tower_data_postgres
servers:
  template-postgres:
    tags:
      - tag_cetmix_tower_data_postgres
    variables:
      postgres_image: "postgres:16"
      postgres_instance: "postgres16"
