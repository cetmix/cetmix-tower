module: cetmix_tower_data_doodba
servers:
  template-doodba:
    variables:
      # Standard server variables
      doodba_name: "{{ odoo_name }}"

      # Doodba server variables
      cetmix_tower_data_odoo.odoo_path_config: "{{ doodba_path }}/conf [add to .gitignore, create conf/odoo.conf]"
