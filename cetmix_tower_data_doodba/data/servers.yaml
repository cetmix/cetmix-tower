module: cetmix_tower_data_doodba
servers:
  template-doodba:
    variables:
      # Standard server variables
      doodba_instance_name: "{{ odoo_instance_name }}"

      # Doodba server variables
      cetmix_tower_data_odoo.odoo_path_config: "{{ doodba_instance_path }}/conf [add to .gitignore, create conf/odoo.conf]"
