<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="cx_tower_plan_line_view_form" model="ir.ui.view">
        <field name="name">cx.tower.plan.line.view.form</field>
        <field name="model">cx.tower.plan.line</field>
        <field
            name="inherit_id"
            ref="cetmix_tower_server.cx_tower_plan_line_view_form"
        />
        <field name="arch" type="xml">
            <xpath expr="//group[@name='main']" position="after">
                <group invisible="not file_template_id">
                    <field name="git_project_id" />
                    <field name="is_make_copy" invisible="not git_project_id" />
                </group>
                <div
                    class="alert alert-info"
                    role="alert"
                    invisible="not file_template_id"
                >
                    <p>
                        Legacy: kept for servers that do not use Jets. New setups
                        should link the Git Project to the Jet and write it with
                        the Upload Git Project command. You can also provide a
                        Git Project reference using the <code>__git_project__</code>
                        variable in the flight plan custom values (also legacy).<br />
                        Python command code example:
                        <code>
custom_values['__git_project__'] = 'my_git_project'
                        </code>
                        <br />
                        Important: if defined, this variable value overrides the Git Project selected in the form.
                    </p>
                </div>
            </xpath>
        </field>
    </record>
</odoo>
