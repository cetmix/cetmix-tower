module: cetmix_tower_data_traefik
servers:
  template-traefik:
    variables:
      # Standard server variables
      traefik_image_name: "{{ traefik_name }}:v3.0"
      traefik_name: "shared-traefik"

      # Traefik server variables
      traefik_email: ""
